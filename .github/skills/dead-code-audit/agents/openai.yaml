interface:
  display_name: 'Dead Code Audit'
  short_description: 'Find evidence of reachability before removal'
  default_prompt: 'Use $dead-code-audit to audit this Grand Transition scope for dead items that have evidence.'
policy:
  allow_implicit_invocation: true
