interface:
  display_name: 'Refactor'
  short_description: 'Simplify structure without a behavior change'
  default_prompt: 'Use $refactor to simplify this Grand Transition scope without a change to behavior.'
policy:
  allow_implicit_invocation: true
