interface:
  display_name: "Generate Raster Art with OpenAI"
  short_description: "Generate Flare art, inspect alpha, and integrate"
  default_prompt: "Use $generate-scene-openai to generate or edit scene and character art. Route transparency, exact masters, and output above 1080p through the repository Flare API helper. Inspect, prepare, and integrate accepted output."
policy:
  allow_implicit_invocation: true
