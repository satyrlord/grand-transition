interface:
  display_name: "Generate Character Art with OpenAI"
  short_description: "Generate office clip-art characters and five-pose packages"
  default_prompt: "Use $generate-character-openai for one declared character work unit. Set review, selection, or package mode. Resume by verified hashes. Never repeat completed or uncertain requests."
policy:
  allow_implicit_invocation: true
