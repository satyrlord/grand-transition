interface:
  display_name: "Run Quality Gate"
  short_description: "Run or repair configured checks"
  default_prompt: "Use $run-quality-gate to run the configured Grand Transition repository checks."
policy:
  allow_implicit_invocation: true
