interface:
  display_name: 'Simulate Matches'
  short_description: 'Run a deterministic match workload of a given size'
  default_prompt: 'Use $simulate-matches with a necessary positive integer match_count to run Grand Transition headless matches.'
policy:
  allow_implicit_invocation: true
